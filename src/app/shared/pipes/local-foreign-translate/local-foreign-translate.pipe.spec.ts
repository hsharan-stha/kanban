import { LocalForeignTranslatePipe } from './local-foreign-translate.pipe';

describe('LocalForeignTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new LocalForeignTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
